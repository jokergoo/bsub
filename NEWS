CHANGES in VERSION 0.0.3

* add `monitor()` which opens a shiny app to monitor jobs

---------------------------------------

CHANGES in VERSION 0.0.2

* add `brecent()` which returns status of n most recent jobs
* `job_logs()` also support finished jobs
* proper environment variables are initialized if via ssh connection
* EXIT can be corrected catched by testing the exit code of the code chunk

---------------------------------------
